[project]
name = "problem-solver"
version = "0.1.0"
description = "Multi-agent problem solver: delegates coding problems, verifies solutions via code execution"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=2.4.0",
    "litellm<1.50.0",
    "exa-py>=2.16.0",
    "python-dotenv>=1.0.0",
]